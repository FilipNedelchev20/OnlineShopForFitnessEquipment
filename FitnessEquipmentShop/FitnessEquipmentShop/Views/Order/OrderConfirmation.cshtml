@model int
@{
    ViewData["Title"] = "Order Confirmation";
}
<div class="mb-3">
    <a href="@Url.Action("Index", "Home")" class="btn btn-light shadow-lg fw-bold">
        ⬅️ Back to Home
    </a>
</div>
<div class="text-center">
    <h2>Thank You for Your Order!</h2>
    <p>Your order has been successfully placed.</p>
    <p><strong>Order ID:</strong> @Model</p>
    <a href="@Url.Action("Index", "Product")" class="btn btn-primary">Continue Shopping</a>
    <a href="@Url.Action("Index", "Order")" class="btn btn-secondary">View Your Orders</a>
</div>
