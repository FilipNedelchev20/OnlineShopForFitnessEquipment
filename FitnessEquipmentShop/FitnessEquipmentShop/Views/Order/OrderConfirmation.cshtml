@model int
@{
    ViewData["Title"] = "Order Confirmation";
}
<div class="mb-3">
    <a href="@Url.Action("Index", "Home")" class="btn btn-light shadow-lg fw-bold">
        ⬅️ Back to Home
    </a>
</div>
<div class="container text-center py-5">
    <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
    <h1 class="mt-3 fw-bold text-success">Thank you for your purchase!</h1>
    <p class="lead">Your order has been placed and is being processed.</p>
    <a href="@Url.Action("Index", "Product")" class="btn btn-primary mt-4 fw-bold px-4 py-2">Continue Shopping 🛒</a>
</div>
